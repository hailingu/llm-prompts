[tool.ruff]
line-length = 88
select = ["E", "F", "W", "C", "Q", "B", "S"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
