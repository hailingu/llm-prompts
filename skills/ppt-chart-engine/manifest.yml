name: ppt-chart-engine
version: 1.0.0
schema_version: 1
description: "Chart engine for PPT HTML slides — chart type catalog, selection algorithm, semantic mapping, data contracts, rendering constraints (Bubble/Line/Heatmap), container height contract, data guards, and code examples."
owner: ppt-html-generator
category: chart-engine
tags:
  - chart
  - chart-selection
  - data-contract
  - rendering
  - bubble-chart
  - heatmap
  - echarts
  - chartjs
  - ppt
dependencies:
  - ppt-brand-system
integration:
  agents:
    - ppt-html-generator
    - ppt-visual-designer
  skills:
    - ppt-brand-system
last_updated: 2026-02-15

readme: README.md
charts: charts.yml

outputs:
  success:
    description: "Chart type resolved, rendering constraints applied, data guards passed, code template ready."
    fields:
      - chart_type
      - engine
      - cdn_url
      - rendering_constraints
      - code_template
